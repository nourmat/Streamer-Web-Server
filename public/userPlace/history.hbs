<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>history</title>
</head>

<body>
    {{#if cameraName}}
        <h1>History of {{cameraName}}</h1>
    {{else}}
        <h1>Opps something went wrong! :(</h1>
    {{/if}}

    <div id="mainBody"></div>

        {{!-- <script type="text/javascript"> 
            var place_ip = {{cameraData.place_ip}};
            var camera_id = {{cameraData.camera_id}}

            fetch('./history?IP=' + place_ip '&Camera_Id=' camera_id)
                .then((res) => {
                    return res.json();
                }).then((data) => {
                    appendData(data);
                }).catch((err) => {
                    console.log(err);
                });

            function appendData(data) {
                var mainContainer = document.getElementById("mainBody");

                for (var i = 0; i < data.length; i++) {
                    var header3 = document.createElement("h3");

                    var anch = document.createElement("a");
                    anch.innerHTML = data[i].Key;
                    anch.href = './history/download?filename=' + data[i].Key;

                    header3.innerHTML = 'Download: ';
                    header3.appendChild(anch);

                    mainContainer.appendChild(header3);
                }
            }
        </script> --}}
</body>

</html>
